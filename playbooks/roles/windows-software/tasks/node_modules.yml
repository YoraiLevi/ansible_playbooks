- block:
  # - name: Install nodejs modules
  #   debug:
  #     msg: "{{args}}"
  - name: Install nodejs modules
    include_role:
      name: npm_install
    vars:
      npm_args: "{{args.name | mandatory}}"
      nodejs_version: "{{args.nodejs_version|default(None)}}"
      use: "{{args.use | default(None)}}"
